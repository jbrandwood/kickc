const void()** KERNEL_IRQ = $0314;
const byte* BGCOL = $d020;
const byte* FGCOL = $d021;

void main() {
    *KERNEL_IRQ = &irq;
    while(true) {
        (*FGCOL)++;
    }
}

interrupt void irq() {
    (*BGCOL)++;
    asm {
        lda $dc0d
    }
}

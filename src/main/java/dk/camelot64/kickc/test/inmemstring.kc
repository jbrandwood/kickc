
byte* SCREEN = $0400;
byte[] TEXT = "camelot ";

main();
void main() {
    byte* cursor = SCREEN;
    byte i=0;
    do {
        (*cursor) = TEXT[i];
        if(++i==8) {
            i = 0;
        }
    } while(++cursor<SCREEN+1000)
}
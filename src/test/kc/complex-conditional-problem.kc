// Test to provoke Exception when using complex || condition

const byte* RASTER = $d012;
const byte* SCREEN = $0400;

void main() {
    while(true) {
        byte key = *RASTER;
        if (key > $20 || key < $40) {
            key = 0;
        }
        *SCREEN = key;
    }
}

import "sinus.kc"
import "print.kc"

void main() {
    word wavelength = 120;
    signed byte[120] sintab2;
    sin8s_gen(sintab2, wavelength);
    print_cls();
    for(byte i: 0..119) {
        signed byte sb = sintab2[i];
        if(sb>=0) {
          print_str(" @");
        }
        print_sbyte(sb);
        print_str("  @");
    }
}

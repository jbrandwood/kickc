// Illustrates how inline assembler can reference data from the outside program

const byte* SCREEN = $400;
byte[] table = "cml!";
void main() {
    asm {
        ldx #0
        !:
        lda table,x
        sta SCREEN+1,x
        inx
        cpx #4
        bne !-
    }

}
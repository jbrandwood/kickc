// Test of simple enum error - name already used

enum State {
        OFF,
        ON,
        OFF
};

void main() {
    enum State state = ON;
    const byte* SCREEN = 0x0400;
    *SCREEN = state;
}


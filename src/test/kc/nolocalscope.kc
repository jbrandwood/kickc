// Illustrates problem with not introducing local scopes inside loops etc

import "print"

void main() {
    for (byte i: 0..5)
        print_ln();
    for (byte i: 0..5)
        print_str_ln(" xxxxx@");
}
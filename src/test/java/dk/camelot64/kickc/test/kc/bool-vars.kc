// A Minimal test of boolean variables.
// Boolean variables are bytes under the hood
// 0: false, !=0 : true

const byte* screen = $400;

void main() {
    for( byte i : 0..20) {
        boolean o1 = i<10;
        boolean o2 = (i&1)==0;
        if( o1 && o2 ) {
            screen[i] = '*';
        } else {
            screen[i] = ' ';
        }
    }
}


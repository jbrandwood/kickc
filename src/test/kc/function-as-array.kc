// Tests treating a function like an array
// Should produce an error

void main() {
    // Add a ball
    new_ball(0);
    new_ball[4];
}

const char[$10] BALLS;

void new_ball(char i) {
    BALLS[i]++;
}
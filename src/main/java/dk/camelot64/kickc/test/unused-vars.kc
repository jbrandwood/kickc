// used vars
const byte* SCREEN = $0400;
byte b=0;

// unused vars
const byte* BGCOL = $d021;
byte[] msg = "hello world@";
byte[] arr = { 7, 8, 9};
byte c=1;
word d=1000;

void main() {
    // used vars
    byte col=2;
    byte* COLS=$d800;
    // unused vars
    byte e=3+3+3;
    word f=2000+2000+d;
    byte[] g = {4, 5, 6};
    byte[] h = "goodbye sky ";

    for(byte i : 0..100) {
        // the last unused var
        signed byte x = -13;
        COLS[i] = col;
        SCREEN[i] = b;
    }

}

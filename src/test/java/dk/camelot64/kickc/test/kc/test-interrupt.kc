void()** KERNEL_IRQ = $0314;
const byte* BGCOL = $d020;
byte* FGCOL = $d021;

void main() {
    *KERNEL_IRQ = &irq;
    while(true) {
        (*FGCOL)++;
    }
}

interrupt(kernel) void irq() {
    (*BGCOL)++;
    asm {
        lda $dc0d
    }
}

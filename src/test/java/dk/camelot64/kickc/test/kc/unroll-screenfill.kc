// Fills the screen using an unrolled inner loop

void main() {
    byte* SCREEN = $400;

    byte a=3;
    inline do {
        SCREEN[a]=a;
        a++;
    } while(a<14);
    SCREEN[a]=a;



/*
    for(byte x: 0..39) {

        inline for(byte line: 0..24) {
            (SCREEN+line*40)[x] = x;
        }

    }
    */

/*
    for(byte x: 0..39) {
     byte line = 0;
     inline while(line!=25 || x<10) {
        (SCREEN+line*40)[x] = x;
        line++;
     }
    }
    */

}
const void()** KERNEL_IRQ = $0314;
const byte* BGCOL = $d020;
volatile byte col = 0;

void main() {
    *KERNEL_IRQ = &irq;
    while(true) {
        col++;
    }
}

interrupt(kernel) void irq() {
    asm {
        lda $dc0d
    }
    *BGCOL = col;
}



void main() {
    for( register(X) byte x: 0..100 ) {
        for( byte y: 0..100 ) {
            for( byte a: 0..100 ) {
                print(y, a+x);
            }
        }
    }
}

void print(byte idx, register(y) byte val) {
    byte* SCREEN  = $0400;
    SCREEN[idx] = val;
}
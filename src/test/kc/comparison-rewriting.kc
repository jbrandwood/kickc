// Test rewriting of constant comparisons

void main() {
    byte* screen = $0400;

    for(byte* sc : screen..screen+1000) *sc=' ';

    byte[] header = "  <  <= == >= >";
    for( byte i=0; header[i]!=0; i++)
        screen[i] = header[i];

    for(byte i=0;i<=9;i++) {
        screen +=40;
        screen[0] = '0'+i;
        if(i<5) screen[2] = '+';
        if(i<=5) screen[5] = '+';
        if(i==5) screen[8] = '+';
        if(i>=5) screen[11] = '+';
        if(i>5) screen[14] = '+';
    }
}
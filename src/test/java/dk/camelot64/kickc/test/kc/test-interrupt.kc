void()** KERNEL_IRQ = $0314;

interrupt(kernel) void irq() {
    byte* BGCOL = $d020;
    (*BGCOL)++;
    asm {
        lda $dc0d
    }
    (*BGCOL)++;
}

void main() {
    *KERNEL_IRQ = &irq;
    byte* FGCOL = $d021;
    while(true) {
        (*FGCOL)++;
    }
}
